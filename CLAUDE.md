# Agent Context & Instructions

Please refer to [AGENTS.md](./AGENTS.md) for full architecture notes, domain-specific rules (RTL PageFlip simulation, Kemenag Arabic font, Audio state machine), API schemas, and commands.

### Quick Commands:
- Dev: `bun run dev`
- Build & Typecheck: `bun run build` (`vue-tsc -b && vite build`)
- Preview: `bun run preview`
